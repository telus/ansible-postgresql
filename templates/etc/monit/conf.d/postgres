check process postgres with pidfile {{ postgres_pidfile_path }}
  start program "/usr/sbin/service postgres start"
  stop program "/usr/sbin/service postgres stop"
  if 5 restarts within 5 cycles then timeout
