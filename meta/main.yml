---
dependencies:
  - role: meta/monitor
    monitor_name: postgres
    monitor_pidfile: "{{ postgres_pidfile_path }}"
    monitor_start_command: /usr/sbin/service postgresql start
    monitor_stop_command: /usr/sbin/service postgresql stop
