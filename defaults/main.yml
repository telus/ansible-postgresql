---
postgres_client: no
postgres_server: no

postgres_user: postgres

postgres_runtime_root: "{{ runtime_root }}/postgres"
postgres_pidfile_path: "{{ postgres_runtime_root }}/postgres.pid"

postgres_accepts_external_connections: yes
